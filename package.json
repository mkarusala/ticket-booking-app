{
  "name": "ticket-booking-app",
  "version": "1.0.0",
  "description": "A simple Node.js backend for the ticket booking service.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Running mock tests... App is healthy.\" && exit 0",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

